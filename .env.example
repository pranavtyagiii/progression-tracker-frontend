# The URL where your backend API is running. During local development this
# is the backend's default; in production, point it at wherever your tech
# team deploys the backend (e.g. https://api.yournst-domain.com).
VITE_API_URL=http://localhost:4000
